"use client"
import {useState} from "react"

export const SplineGsap = ({ children } : { children: React.ReactNode }) => {
    return(
        <>
            <div>{ children } </div>
        </>
    )
}